- name: loop
  hosts: localhost
  vars:
     my_vms:
       - home-lab-9-065
       - home-lab-9-066
       - home-lab-9-067
  tasks:
     - name: bash test
       shell:
         ping -c 3 "{{ item }}"
       loop: "{{ my_vms }}"
